package xiao.battleroyale.common.game.stats;

import com.google.gson.JsonArray;
import org.jetbrains.annotations.NotNull;

public class GameEventStatsHelper {

    protected static void addTimelineStats(StatsManager statsManager, @NotNull JsonArray jsonArray) {
        ;
    }

    protected static void addRankStats(StatsManager statsManager, @NotNull JsonArray jsonArray) {
        ;
    }

    protected static void addDetailStats(StatsManager statsManager, @NotNull JsonArray jsonArray) {
        ;
    }
}
