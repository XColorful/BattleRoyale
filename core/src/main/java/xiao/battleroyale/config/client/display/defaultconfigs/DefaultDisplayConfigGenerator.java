package xiao.battleroyale.config.client.display.defaultconfigs;

public class DefaultDisplayConfigGenerator {

    public static void generateAllDefaultConfig(String configDirPath) {
        generateDefaultDisplayConfigs(configDirPath);
    }

    public static void generateDefaultDisplayConfigs(String configDirPath) {
        DefaultDisplay.generateDefaultConfigs(configDirPath);
    }
}
