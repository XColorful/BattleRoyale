modLoader="javafml"
loaderVersion="${loader_version_range}"
license="${mod_license}"
issueTrackerURL="${mod_issue_url}"

[[mods]]
modId="${mod_id}"
version="${mod_version}"
displayName="${mod_name}"
displayURL="${mod_display_url}"
logoFile="${mod_logo_file}"
credits="${mod_credits}"
authors="${mod_authors}"
description='''${mod_description}'''

[[dependencies.${mod_id}]]
modId="${target_modloader}"
mandatory=true
versionRange="${target_version_range}"
ordering="NONE"
side="BOTH"

[[dependencies.${mod_id}]]
modId="minecraft"
mandatory=true
versionRange="${minecraft_version_range}"
ordering="NONE"
side="BOTH"

[[dependencies.${mod_id}]]
modId="journeymap"
mandatory=false
versionRange="${journeymap_version_range}"
ordering="NONE"
side="CLIENT"

[[dependencies.${mod_id}]]
modId="playerrevive"
mandatory=false
versionRange="${playerrevive_version_range}"
side="BOTH"

[[dependencies.${mod_id}]]
modId="tacz"
mandatory=false
versionRange="${tacz_version_range}"
side="BOTH"

[[dependencies.${mod_id}]]
modId="cbraddon"
mandatory=false
versionRange="${cbraddon_version_range}"
side="SERVER"

[[dependencies.${mod_id}]]
modId="armorscaling"
mandatory=false
versionRange="${armorscaling_version_range}"
side="SERVER"
